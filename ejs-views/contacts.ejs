<!DOCTYPE html>
<html lang="en">
    <%- include('./partials/head.ejs')%>
<body>
    <%- include('./partials/nav.ejs')%>
    
    <ul>
       <% if (contacts.length) {  %>
           <% contacts.forEach(({link, name}) => { %>
               <li> 
                 <a href="<%= link %>"><%= name %></a>
               </li>
           <% }); %> <!-- Вот пропущенный закрывающий тег %>-->
       <% } %>
    </ul>
    
</body>
</html>


